# 🎁 기부 마을 (Donation Village) 발표 자료

> **슬로건: 투명한 선의, 따뜻한 기부**

---

## 📋 목차

1. **도입부** - "기부로 게임을?"
2. **개발 배경** - 왜 블록체인인가?
3. **기술적 고민과 해결**
4. **블록체인 기술 스택**
5. **스마트 컨트랙트 구조**
6. **DID & Verifiable Credential**
7. **시스템 아키텍처**
8. **프론트엔드 & 백엔드**
9. **배포 과정**
10. **향후 발전 가능성**

---

## 1️⃣ 도입부: "기부로 게임을?"

### 💭 질문으로 시작

> "기부를 게임처럼 즐긴다면 어떨까요?"

### 🎮 우리의 답

**기부 마을 (Donation Village)**
- 동물의 숲 스타일 힐링 게임
- 기부할 때마다 마을이 성장
- 집 꾸미기, 캐릭터 꾸미기
- **하지만 모든 기부는 블록체인에 영구 기록**

### 슬라이드 구성

```
┌─────────────────────────────────┐
│   기부로 게임을?               │
│                                 │
│   [게임 스크린샷]              │
│   - 귀여운 마을 화면            │
│   - 집들이 모여있는 모습        │
│                                 │
│   "게임하듯 기부하고,           │
│    블록체인으로 투명하게"       │
└─────────────────────────────────┘
```

---

## 2️⃣ 개발 배경: 왜 블록체인인가?

### 😰 문제 상황

**실제 사례들:**
```
📰 불법 기부금 탈취 사건
📰 투명하지 않은 회계 처리
📰 기부금 용도 불명확
📰 중간 관리 비용 과다
```

### 🔍 왜 기존 방식은 한계가 있었나?

#### ❌ 원화 통장 방식의 문제점

1. **유동성이 심함**
   ```
   원화 → 은행 계좌 → 통장 내역
   문제: 통장 조작 가능, 중간 과정 불투명
   ```

2. **원화 거래 내역을 온체인에 올리기 어려움**
   ```
   이론: 은행 API → 서버 → 블록체인
   
   문제점:
   ✗ 신뢰 주체가 다시 은행+서버로 돌아감
   ✗ 은행 API 권한/보안/규제 문제
   ✗ 실제 계좌와 자동 연동 거의 불가능
   ✗ 중복 기록 (은행 장부 + 블록체인)
   ```

3. **기존 유사 플랫폼 존재**
   - '체리(Cherry)' 등 기부 플랫폼 이미 존재
   - 차별화 필요성 대두

### ✅ 우리의 해결책

**"기부 흐름 자체를 블록체인으로 끌어올리기"**

```
┌────────────────────────────────────────┐
│  기존 방식:                            │
│  원화 → 은행 → 단체 → 사용            │
│  (불투명한 여러 단계)                  │
├────────────────────────────────────────┤
│  우리 방식:                            │
│  암호화폐 → 스마트컨트랙트 → 사용     │
│  (모든 과정이 블록체인에 기록)         │
└────────────────────────────────────────┘
```

**+ 차별화: 게임화 (Gamification)**
- 힐링 게임 '동물의 숲'에서 영감
- 기부 = 게임 플레이
- 포인트로 집 꾸미기, 뱃지 수집
- 투명성 + 재미 동시에 확보

### 🎯 블록체인의 핵심 특성 활용

```
┌─────────────────────────────────────────┐
│ ✅ 무결성 (Immutability)                │
│    → 기부 기록 영구 보존, 조작 불가    │
│                                         │
│ ✅ 투명성 (Transparency)                │
│    → 누구나 검증 가능한 거래 내역      │
│                                         │
│ ✅ 신뢰성 (Trustless)                   │
│    → 중앙 기관 없이도 신뢰 확보        │
└─────────────────────────────────────────┘
```

---

## 3️⃣ 기술적 고민과 해결

### 💡 주요 기술 결정 사항

#### 1. **원화 vs 암호화폐**
```
고민: 일반 사용자는 원화를 쓴다

결론: 암호화폐 기부로 시작
이유:
  ✓ 진정한 블록체인 투명성
  ✓ 스마트컨트랙트로 자동화
  ✓ 중간 조작 불가능
  ✓ 글로벌 기부 가능

향후: 스테이블코인(USDT 등) 지원으로
      원화 감각 제공 가능
```

#### 2. **메타마스크 없이 어떻게?**
```
문제: 일반 사용자는 메타마스크 사용 어려움

해결: 자체 인증 시스템 구축
  ✓ 이메일만으로 로그인
  ✓ 백엔드에서 지갑 자동 생성
  ✓ 사용자는 암호화폐 지식 불필요
  ✓ DID로 신원 관리
```

#### 3. **Layer 2를 선택한 이유**
```
고민: 이더리움 메인넷은 가스비가 비싸다

해결: Arbitrum (Layer 2) 사용
  ✓ 낮은 가스비 (메인넷의 1/10~1/100)
  ✓ 빠른 트랜잭션 속도
  ✓ 이더리움 보안성 그대로 유지
  ✓ 실제 기부 사용에 적합
```

---

## 4️⃣ 블록체인 기술 스택

### 🛠️ 스마트 컨트랙트 개발

```javascript
┌──────────────────────────────────┐
│ 언어: Solidity ^0.8.20           │
│ 프레임워크: Hardhat 2.22.0       │
│ 라이브러리: OpenZeppelin 5.0.0   │
│ 네트워크: Arbitrum Sepolia       │
│ 도구: Ethers.js v6               │
└──────────────────────────────────┘
```

#### 사용한 OpenZeppelin 라이브러리

```solidity
import "@openzeppelin/contracts/access/Ownable.sol";
import "@openzeppelin/contracts/utils/ReentrancyGuard.sol";
import "@openzeppelin/contracts/utils/Pausable.sol";
```

**왜 이것들을?**
- `Ownable`: 관리자 권한 관리 (캠페인 생성/중지)
- `ReentrancyGuard`: 재진입 공격 방지 (보안)
- `Pausable`: 긴급 상황 대응 (일시 중지)

### 🌐 네트워크: Arbitrum Sepolia 선택 이유

```
┌─────────────────────────────────────────┐
│ Arbitrum Sepolia (Layer 2 테스트넷)     │
├─────────────────────────────────────────┤
│ ✅ 낮은 가스비                          │
│    - 이더리움 메인넷 대비 1/10~1/100    │
│    - 기부 플랫폼에 적합                 │
│                                         │
│ ✅ 빠른 속도                            │
│    - 트랜잭션 확정 시간 ~1초            │
│    - 사용자 경험 향상                   │
│                                         │
│ ✅ 이더리움 보안                        │
│    - Rollup 기술로 메인넷 보안 유지     │
│    - 탈중앙화 보장                      │
│                                         │
│ ✅ 실전 배포 가능                       │
│    - Sepolia(테스트) → One(메인넷)      │
│    - 코드 변경 없이 전환 가능           │
└─────────────────────────────────────────┘
```

**Hardhat 설정:**
```javascript
networks: {
  arbitrumSepolia: {
    url: "https://sepolia-rollup.arbitrum.io/rpc",
    chainId: 421614
  }
}
```

### 🔑 DID (Decentralized Identifier)

#### 왜 DID를 사용했나?

```
문제: 메타마스크 없이 어떻게 신원 관리?

해결: DID 표준 활용
  ✓ 탈중앙화 신원 증명
  ✓ 지갑 주소 = 신원
  ✓ W3C 표준 준수
```

#### DID 구조

```
did:ethr:arbitrum-sepolia:0x1234...5678
│   │    │                 │
│   │    │                 └─ 지갑 주소
│   │    └─ 네트워크 (Arbitrum Sepolia)
│   └─ 방법 (Ethereum)
└─ DID 프로토콜
```

#### DID Document 예시

```json
{
  "@context": "https://www.w3.org/ns/did/v1",
  "id": "did:ethr:arbitrum-sepolia:0x1234...5678",
  "verificationMethod": [{
    "id": "did:ethr:arbitrum-sepolia:0x1234...5678#controller",
    "type": "EcdsaSecp256k1RecoveryMethod2020",
    "controller": "did:ethr:arbitrum-sepolia:0x1234...5678",
    "blockchainAccountId": "eip155:421614:0x1234...5678"
  }],
  "authentication": ["did:ethr:arbitrum-sepolia:0x1234...5678#controller"],
  "service": [{
    "id": "#donation-ledger",
    "type": "DonationLedger",
    "serviceEndpoint": "0xabcd...efgh"  // 컨트랙트 주소
  }]
}
```

### 📜 Verifiable Credential (VC)

#### 기부 증명서를 블록체인 자격증명으로

```json
{
  "@context": [
    "https://www.w3.org/2018/credentials/v1",
    "https://www.donation-village.org/credentials/v1"
  ],
  "type": ["VerifiableCredential", "DonationCredential"],
  "issuer": "did:ethr:arbitrum-sepolia:0xCONTRACT",
  "issuanceDate": "2025-12-02T00:00:00Z",
  "credentialSubject": {
    "id": "did:ethr:arbitrum-sepolia:0xUSER",
    "donationAmount": "0.1 ETH",
    "donationCount": 5,
    "campaignId": "1",
    "txHash": "0xabc123...",
    "timestamp": "2025-12-02T00:00:00Z"
  },
  "proof": {
    "type": "EthereumEip712Signature2021",
    "created": "2025-12-02T00:00:00Z",
    "proofPurpose": "assertionMethod",
    "verificationMethod": "0xUSER",
    "signature": "0xdef456..."
  }
}
```

**활용:**
- 기부 이력 증명
- 뱃지 발급 (Soul Bound Token 개념)
- 타 플랫폼에서 검증 가능

### 🔐 메타마스크 없이 구현하기

#### 기존 문제
```
❌ 메타마스크 설치 필요
❌ 니모닉 관리 부담
❌ 트랜잭션 승인 매번 클릭
❌ 일반 사용자 진입 장벽 높음
```

#### 우리의 해결책

```typescript
// 1. 이메일로 결정론적 지갑 생성
const seed = ethers.id(email);  // 이메일 → 시드
const wallet = new ethers.Wallet(seed);

// 2. 자동으로 DID 발급
const did = `did:ethr:arbitrum-sepolia:${wallet.address}`;

// 3. 백엔드에서 트랜잭션 처리
const signer = wallet.connect(provider);
const tx = await contract.donate(campaignId, { value: amount });
```

**장점:**
```
✅ 이메일만 입력
✅ 지갑 자동 생성
✅ 메타마스크 불필요
✅ 일반 웹사이트처럼 사용
✅ 하지만 진짜 블록체인!
```

**보안:**
- 프라이빗 키는 로컬스토리지에 암호화 저장
- 실제 서비스에서는 AWS KMS 등 사용 예정

---

## 5️⃣ 스마트 컨트랙트 구조

### 📐 DonationVillage.sol 설계

#### 핵심 구조체

```solidity
struct Campaign {
    string organizationName;   // 단체명
    string title;              // 캠페인 제목
    string description;        // 설명
    string category;           // 카테고리 (동물/환경/교육)
    uint256 goalAmount;        // 목표 금액
    uint256 currentAmount;     // 현재 모금액
    address payable beneficiary; // 수혜자 주소
    bool active;               // 활성화 상태
    uint256 createdAt;         // 생성 시간
}

struct Donation {
    address donor;             // 기부자 주소
    uint256 campaignId;        // 캠페인 ID
    uint256 amount;            // 기부 금액
    uint256 timestamp;         // 기부 시간
    string message;            // 응원 메시지
}
```

#### 주요 함수

```solidity
// 1. 캠페인 생성 (관리자만)
function createCampaign(...) external onlyOwner

// 2. 기부하기 (누구나)
function donate(uint256 campaignId, string message) 
    external payable nonReentrant whenNotPaused

// 3. 기부금 인출 (수혜자만)
function withdrawFunds(uint256 campaignId) 
    external nonReentrant

// 4. 조회 함수들
function getUserDonations(address user) view
function getCampaignDonations(uint256 campaignId) view
function getTotalDonated(address user) view
```

### 🔒 보안 설계

```solidity
contract DonationVillage is 
    Ownable,         // 관리자 권한 관리
    ReentrancyGuard, // 재진입 공격 방지
    Pausable         // 긴급 중지 기능
{
    // 기부 함수는 3중 보호
    function donate(...) 
        external payable 
        nonReentrant      // ① 재진입 방지
        whenNotPaused     // ② 일시 중지 체크
    {
        require(msg.value > 0, "..."); // ③ 입력 검증
        // ...
    }
}
```

### 📊 이벤트 설계 (투명성)

```solidity
event DonationMade(
    uint256 indexed donationId,
    uint256 indexed campaignId,
    address indexed donor,
    uint256 amount,
    uint256 timestamp
);

event FundsWithdrawn(
    uint256 indexed campaignId,
    address indexed beneficiary,
    uint256 amount
);
```

**왜 이벤트가 중요한가?**
- 모든 거래를 추적 가능
- 프론트엔드에서 실시간 감지
- 블록체인 탐색기에서 확인 가능

### 🎯 설계 철학

```
┌─────────────────────────────────────┐
│ 1. 단순성 (Simplicity)              │
│    - 복잡한 로직 지양               │
│    - 가독성 높은 코드               │
│                                     │
│ 2. 보안 우선 (Security First)       │
│    - OpenZeppelin 검증된 라이브러리 │
│    - 3중 보안 체크                  │
│                                     │
│ 3. 가스 최적화 (Gas Optimization)   │
│    - Optimizer enabled (200 runs)   │
│    - 불필요한 저장 최소화           │
│                                     │
│ 4. 투명성 (Transparency)            │
│    - 모든 중요 동작에 이벤트 발생   │
│    - public/view 함수로 조회 가능   │
└─────────────────────────────────────┘
```

---

## 6️⃣ 시스템 아키텍처

### 🏗️ 전체 구조도

```
┌─────────────────────────────────────────────────────────────┐
│                        사용자 (User)                         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ 이메일 로그인
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   프론트엔드 (React + Vite)                  │
├─────────────────────────────────────────────────────────────┤
│  • VillageMain (마을 화면)                                   │
│  • OrganizationHouse (단체 집 방문)                          │
│  • DonationModal (기부하기)                                  │
│  • MyHouse (집 꾸미기 + DID 확인)                            │
│  • Inventory (아이템 구매)                                   │
└────────────┬────────────────────────┬───────────────────────┘
             │                        │
             │ REST API               │ ethers.js (직접 호출)
             ▼                        ▼
┌─────────────────────────┐  ┌──────────────────────────────┐
│  백엔드 (Supabase)      │  │  블록체인 (Arbitrum Sepolia)  │
├─────────────────────────┤  ├──────────────────────────────┤
│ • Hono 웹서버           │  │ • DonationVillage.sol         │
│ • KV Store (Postgres)   │  │   - donate()                  │
│ • Auth (이메일)         │  │   - withdrawFunds()           │
│ • Storage (프로필 사진) │  │   - getCampaign()             │
│                         │  │                               │
│ 저장 데이터:            │  │ 저장 데이터:                  │
│ - 사용자 포인트         │  │ - 기부 트랜잭션 (영구)        │
│ - 집 인테리어           │  │ - 캠페인 정보                 │
│ - 아이템 소유 여부      │  │ - 총 기부 금액                │
│ - 뱃지 획득 내역        │  │ - 타임스탬프                  │
└─────────────────────────┘  └──────────────────────────────┘
```

### 🔄 기부 플로우

```
[사용자] 
   │
   │ ① "기부하기" 버튼 클릭
   ▼
[프론트엔드]
   │
   │ ② 금액 입력 + 메시지 작성
   ▼
[ethers.js]
   │
   │ ③ 자동 생성된 지갑으로 트랜잭션 서명
   ▼
[Arbitrum Sepolia]
   │
   │ ④ DonationVillage.donate() 실행
   │    - 기부 기록 블록체인에 저장
   │    - DonationMade 이벤트 발생
   ▼
[프론트엔드]
   │
   │ ⑤ 트랜잭션 해시 획득
   │ ⑥ VC (Verifiable Credential) 생성
   │ ⑦ localStorage에 증명서 저장
   ▼
[백엔드 API]
   │
   │ ⑧ 포인트 지급 (기부액의 10%)
   │ ⑨ 사용자 데이터 업데이트
   ▼
[프론트엔드]
   │
   │ ⑩ "기부 완료!" 알림 표시
   │ ⑪ 마이하우스에서 VC 확인 가능
   ▼
[사용자]
   "포인트로 집 꾸미기!"
```

### 📦 데이터 레이어 분리

```
┌──────────────────────────────────────────────────────┐
│                  On-Chain (블록체인)                  │
├──────────────────────────────────────────────────────┤
│ ✅ 기부 트랜잭션 (금액, 시간, 주소)                   │
│ ✅ 캠페인 정보 (목표액, 현재액)                       │
│ ✅ 기부자 총액                                        │
│                                                      │
│ 특징: 영구 보존, 조작 불가, 투명성                   │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│                  Off-Chain (Supabase)                │
├──────────────────────────────────────────────────────┤
│ ✅ 사용자 포인트                                      │
│ ✅ 집 인테리어 배치                                   │
│ ✅ 아이템 소유 여부                                   │
│ ✅ 뱃지 획득 내역                                     │
│                                                      │
│ 특징: 빠른 조회, 유연한 변경, 낮은 비용               │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│              Local Storage (브라우저)                 │
├──────────────────────────────────────────────────────┤
│ ✅ DID & DID Document                                 │
│ ✅ Verifiable Credentials (기부 증명서)               │
│ ✅ 지갑 프라이빗 키 (암호화)                          │
│                                                      │
│ 특징: 사용자 소유, 언제든 내보내기 가능               │
└──────────────────────────────────────────────────────┘
```

**왜 이렇게 나눴나?**

```
온체인 = 절대 변경되면 안 되는 것
  예: 기부 기록, 금액

오프체인 = 자주 바뀌고 빠르게 조회해야 하는 것
  예: 포인트, 인테리어

로컬 = 사용자 개인이 소유해야 하는 것
  예: DID, 증명서, 프라이빗 키
```

---

## 7️⃣ 프론트엔드 & 백엔드 스택

### 🎨 프론트엔드

```typescript
┌──────────────────────────────────┐
│ React 18 + TypeScript            │
│ Vite (빌드 도구)                 │
│ Tailwind CSS (스타일링)          │
│ ethers.js v6 (블록체인 통신)     │
│ Lucide React (아이콘)            │
│ Supabase Client (백엔드 통신)    │
└──────────────────────────────────┘
```

**주요 컴포넌트:**
```
App.tsx
├── LoginScreen          (로그인 화면)
├── VillageMain          (마을 메인 화면)
│   ├── 마을 배경
│   ├── 단체 집들
│   └── 네비게이션
├── OrganizationHouse    (단체 집 상세)
│   ├── 캠페인 정보
│   ├── 진행률 바
│   └── 기부하기 버튼
├── DonationModal        (기부 모달)
│   ├── 금액 입력
│   ├── 메시지 작성
│   └── 트랜잭션 처리
├── MyHouse              (마이하우스)
│   ├── 프로필
│   ├── 기부 내역
│   ├── DID & VC 확인    ⭐
│   └── 뱃지 컬렉션
└── Inventory            (상점)
    ├── 가구 구매
    └── 캐릭터 꾸미기
```

### ⚙️ 백엔드

```typescript
┌──────────────────────────────────┐
│ Supabase Edge Functions          │
│ Hono (웹 프레임워크)              │
│ PostgreSQL (KV Store)             │
│ Supabase Auth (인증)              │
│ Supabase Storage (파일 저장)     │
└──────────────────────────────────┘
```

**API 엔드포인트:**
```typescript
// supabase/functions/server/index.tsx

app.post('/make-server-17e2e0df/donate', async (c) => {
  // 기부 후 포인트 지급
  const { walletAddress, amount } = await c.req.json();
  const points = Math.floor(amount * 0.1); // 10% 포인트
  
  await kv.set(`user:${walletAddress}:points`, points);
  return c.json({ success: true, points });
});

app.get('/make-server-17e2e0df/user/:address', async (c) => {
  // 사용자 데이터 조회
  const address = c.req.param('address');
  const points = await kv.get(`user:${address}:points`);
  
  return c.json({ points });
});
```

### 🔐 인증 시스템

```typescript
// lib/auth-context.tsx

// 1. 이메일로 로그인
const login = async (email: string) => {
  // 결정론적 지갑 생성
  const seed = ethers.id(email);
  const wallet = new ethers.Wallet(seed);
  
  // DID 생성
  const did = `did:ethr:arbitrum-sepolia:${wallet.address}`;
  
  // localStorage에 저장
  localStorage.setItem('user', JSON.stringify({
    email,
    walletAddress: wallet.address,
    did
  }));
};
```

### 🌐 블록체인 통신

```typescript
// lib/contract.ts

import { ethers } from 'ethers';

// Arbitrum Sepolia 연결
const provider = new ethers.JsonRpcProvider(
  'https://sepolia-rollup.arbitrum.io/rpc'
);

// 컨트랙트 인스턴스
const contract = new ethers.Contract(
  CONTRACT_ADDRESS,
  ABI,
  signer
);

// 기부하기
export async function donate(
  campaignId: number, 
  amount: string, 
  message: string
) {
  const tx = await contract.donate(campaignId, message, {
    value: ethers.parseEther(amount)
  });
  
  const receipt = await tx.wait();
  return receipt.hash;
}
```

---

## 8️⃣ 배포 과정

### 📝 스마트 컨트랙트 배포

```bash
# 1. 컴파일
npx hardhat compile

# 2. Arbitrum Sepolia 배포
npx hardhat run scripts/deploy.js --network arbitrumSepolia

# 3. 컨트랙트 검증 (Arbiscan)
npx hardhat verify --network arbitrumSepolia <CONTRACT_ADDRESS>
```

**deploy.js:**
```javascript
async function main() {
  const DonationVillage = await ethers.getContractFactory("DonationVillage");
  const contract = await DonationVillage.deploy();
  
  await contract.waitForDeployment();
  
  console.log("✅ 컨트랙트 배포:", await contract.getAddress());
  console.log("🌐 Arbiscan:", `https://sepolia.arbiscan.io/address/${await contract.getAddress()}`);
}
```

### 🚀 프론트엔드 배포

```bash
# 1. 환경 변수 설정
VITE_CONTRACT_ADDRESS=0x...
VITE_ARBITRUM_RPC=https://sepolia-rollup.arbitrum.io/rpc

# 2. 빌드
npm run build

# 3. Figma Make / Vercel / Netlify 배포
# (정적 파일 호스팅)
```

### ☁️ 백엔드 배포

```bash
# Supabase는 자동 배포
# Edge Functions는 Git push 시 자동 배포됨

# 또는 수동 배포:
supabase functions deploy server
```

### 📋 배포 체크리스트

```
✅ 1. 스마트 컨트랙트 배포
   - Arbitrum Sepolia에 배포
   - Arbiscan 검증
   - 컨트랙트 주소 기록

✅ 2. 환경 변수 설정
   - VITE_CONTRACT_ADDRESS
   - SUPABASE_URL
   - SUPABASE_ANON_KEY

✅ 3. 프론트엔드 빌드 & 배포
   - npm run build
   - dist/ 폴더 호스팅

✅ 4. 백엔드 확인
   - Supabase Functions 작동 확인
   - API 엔드포인트 테스트

✅ 5. 테스트
   - 로그인 테스트
   - 기부 트랜잭션 테스트
   - DID/VC 생성 확인
   - 포인트 지급 확인
```

---

## 9️⃣ 코드 하이라이트 (발표 시 보여줄 부분)

### 💡 1. 기부 함수 (스마트 컨트랙트)

```solidity
// DonationVillage.sol

function donate(uint256 _campaignId, string memory _message) 
    external payable 
    nonReentrant    // 재진입 공격 방지
    whenNotPaused   // 긴급 중지 체크
{
    require(_campaignId > 0 && _campaignId <= campaignCount, 
            "Invalid campaign ID");
    require(msg.value > 0, 
            "Donation amount must be greater than 0");
    
    Campaign storage campaign = campaigns[_campaignId];
    require(campaign.active, "Campaign is not active");
    
    // 기부 기록 저장
    donationCount++;
    donations[donationCount] = Donation({
        donor: msg.sender,
        campaignId: _campaignId,
        amount: msg.value,
        timestamp: block.timestamp,
        message: _message
    });
    
    // 캠페인 금액 업데이트
    campaign.currentAmount += msg.value;
    
    // 사용자 기부 내역 저장
    userDonations[msg.sender].push(donationCount);
    totalDonatedByUser[msg.sender] += msg.value;
    
    // 이벤트 발생 (투명성)
    emit DonationMade(
        donationCount, 
        _campaignId, 
        msg.sender, 
        msg.value, 
        block.timestamp
    );
}
```

**설명 포인트:**
- `nonReentrant`: 보안
- `require`: 입력 검증
- `emit`: 투명성

### 💡 2. DID 생성 (프론트엔드)

```typescript
// lib/did.ts

export function createDID(
  address: string, 
  chainId: number = 421614
): string {
  const network = chainId === 421614 
    ? 'arbitrum-sepolia' 
    : 'arbitrum';
    
  return `did:ethr:${network}:${address.toLowerCase()}`;
}

export function createDIDDocument(
  address: string,
  contractAddress: string,
  chainId: number = 421614
): DIDDocument {
  const did = createDID(address, chainId);
  
  return {
    '@context': 'https://www.w3.org/ns/did/v1',
    id: did,
    verificationMethod: [{
      id: `${did}#controller`,
      type: 'EcdsaSecp256k1RecoveryMethod2020',
      controller: did,
      blockchainAccountId: `eip155:${chainId}:${address}`
    }],
    authentication: [`${did}#controller`],
    service: [{
      id: `${did}#donation-ledger`,
      type: 'DonationLedger',
      serviceEndpoint: contractAddress
    }]
  };
}
```

**설명 포인트:**
- W3C DID 표준
- 블록체인 기반 신원
- 컨트랙트와 연결

### 💡 3. VC (Verifiable Credential) 발급

```typescript
// lib/did.ts

export async function createDonationCredential(
  donorAddress: string,
  contractAddress: string,
  donationAmount: string,
  campaignId: string,
  txHash: string
): Promise<DonationCredential> {
  const donorDID = createDID(donorAddress);
  const issuerDID = createDID(contractAddress);
  
  return {
    '@context': [
      'https://www.w3.org/2018/credentials/v1',
      'https://www.donation-village.org/credentials/v1'
    ],
    type: ['VerifiableCredential', 'DonationCredential'],
    issuer: issuerDID,  // 컨트랙트가 발급
    issuanceDate: new Date().toISOString(),
    credentialSubject: {
      id: donorDID,  // 기부자 DID
      donationAmount,
      campaignId,
      txHash,  // 블록체인 증명
      timestamp: new Date().toISOString()
    }
  };
}
```

**설명 포인트:**
- 기부 기록을 자격증명으로
- 타 플랫폼에서 검증 가능
- 블록체인 트랜잭션 해시 포함

---

## 🔟 향후 발전 가능성

### 🎨 1. NFT 아이템 시스템

```
현재:
  기부 → 포인트 → 아이템 구매 (오프체인)

향후:
  기부 → NFT 민팅 → 온체인 소유권
  
  ✅ 아이템을 NFT로 변환
  ✅ 마켓플레이스에서 거래 가능
  ✅ 다른 게임/플랫폼으로 이전 가능
  ✅ 희귀 아이템 = 높은 기부 증명
```

**기술 구현:**
```solidity
// ERC-721 NFT 컨트랙트
contract VillageItems is ERC721 {
    struct Item {
        string name;
        string imageURI;
        uint256 rarity;
        uint256 unlockedByDonation; // 기부 금액
    }
    
    function mintItem(address to, uint256 itemId) external {
        require(
            getTotalDonated(to) >= items[itemId].unlockedByDonation,
            "Not enough donations"
        );
        _mint(to, itemId);
    }
}
```

### 🌐 2. 메타버스 전환

```
현재:
  2D 탑다운 뷰
  단방향 상호작용

향후:
  3D 메타버스 (Unity/Unreal/Decentraland)
  실시간 소통
  
  ✅ 아바타로 마을 돌아다니기
  ✅ 다른 기부자와 채팅
  ✅ 단체 담당자와 실시간 Q&A
  ✅ 기부 이벤트 참여 (콘서트, 전시회)
  ✅ 기부 리더보드 (순위표)
```

**활용 사례:**
```
예: "숲속동물보호센터" 기부 이벤트
  → 메타버스에서 보호소 투어
  → VR로 동물들 직접 체험
  → 실시간 기부 → 즉시 NFT 뱃지
```

### 🤝 3. 커뮤니티 DAO

```
현재:
  관리자가 캠페인 생성

향후:
  커뮤니티 투표로 결정
  
  ✅ 기부자 = DAO 멤버
  ✅ 신규 캠페인 제안 투표
  ✅ 기부금 사용처 투표
  ✅ 투명한 집행 과정
```

**기술 구현:**
```solidity
contract DonationDAO {
    struct Proposal {
        string description;
        uint256 votesFor;
        uint256 votesAgainst;
        bool executed;
    }
    
    // 기부자만 투표 가능
    function vote(uint256 proposalId, bool support) external {
        require(getTotalDonated(msg.sender) > 0, "Must be a donor");
        // 투표 로직...
    }
}
```

### 💰 4. 스테이블코인 지원

```
현재:
  ETH로만 기부 (가격 변동성)

향후:
  USDT, USDC 등 스테이블코인 지원
  
  ✅ 원화 감각으로 기부 가능
  ✅ 가격 변동 걱정 없음
  ✅ 더 많은 사용자 유입
```

### 🌍 5. 글로벌 확장

```
현재:
  한국 중심

향후:
  전 세계 기부 플랫폼
  
  ✅ 다국어 지원
  ✅ 국제 단체 참여
  ✅ 국경 없는 기부
  ✅ 블록체인의 장점 극대화
```

### 📱 6. 모바일 앱

```
현재:
  웹 브라우저

향후:
  네이티브 앱 (iOS/Android)
  
  ✅ 푸시 알림 (기부 요청, 이벤트)
  ✅ 위치 기반 기부 (근처 캠페인)
  ✅ AR로 마을 구경
  ✅ 더 나은 UX
```

### 🎯 로드맵 요약

```
┌─────────────────────────────────────────────┐
│ Phase 1 (현재) - MVP                        │
│ ✅ 블록체인 기부 기록                       │
│ ✅ 게임화 (포인트, 아이템)                  │
│ ✅ DID/VC 구현                              │
├─────────────────────────────────────────────┤
│ Phase 2 (3개월) - NFT & 커뮤니티            │
│ 🔲 NFT 아이템 시스템                        │
│ 🔲 DAO 투표 기능                            │
│ 🔲 스테이블코인 지원                        │
├─────────────────────────────────────────────┤
│ Phase 3 (6개월) - 메타버스                  │
│ 🔲 3D 메타버스 전환                         │
│ 🔲 실시간 소통 기능                         │
│ 🔲 모바일 앱 출시                           │
├─────────────────────────────────────────────┤
│ Phase 4 (1년) - 글로벌 & 확장               │
│ 🔲 다국어 지원                              │
│ 🔲 국제 NGO 파트너십                        │
│ 🔲 메인넷 배포 (Arbitrum One)               │
└─────────────────────────────────────────────┘
```

---

## 📊 발표 팁

### 🎤 각 슬라이드별 추천 시간 배분

```
1. 도입부 (1분)
   - "기부로 게임을?" 임팩트 있게

2. 개발 배경 (2분)
   - 문제 상황 → 블록체인 해결책

3. 기술 스택 (3분)
   - Arbitrum, DID, Hardhat 강조

4. 스마트 컨트랙트 (3분)
   - 코드 보여주며 설명
   - 보안 설계 강조

5. 아키텍처 (2분)
   - 다이어그램 중심

6. DID/VC (2분)
   - 실제 예시 보여주기

7. 향후 계획 (2분)
   - NFT, 메타버스 비전 제시

총 15분 + Q&A 5분 = 20분
```

### 💡 발표 시 강조할 포인트

```
✨ 1. 투명성
   "모든 기부가 블록체인에 영구 기록됩니다"

✨ 2. 재미
   "게임하듯 기부하고, 집도 꾸미고"

✨ 3. 기술력
   "DID, VC, Layer 2 - 최신 블록체인 기술"

✨ 4. 실용성
   "메타마스크 없이도 누구나 쉽게"

✨ 5. 확장성
   "NFT, 메타버스로 발전 가능"
```

### 🎬 데모 시나리오

```
1. 로그인 화면
   "이메일만 입력하면..."

2. 자동 지갑 생성
   "백그라운드에서 DID 자동 발급"

3. 마을 돌아다니기
   "귀여운 집들이 각 단체를 나타냅니다"

4. 기부하기
   "버튼 클릭 → 트랜잭션 → Arbiscan 확인"

5. DID & VC 확인
   "마이하우스에서 증명서 확인 가능"

6. Arbiscan 보여주기
   "실제 블록체인에 기록된 걸 확인하세요!"
```

---

## 📌 결론 슬라이드

```
┌─────────────────────────────────────────┐
│   투명한 선의, 따뜻한 기부               │
│                                         │
│   기부 마을 (Donation Village)          │
├─────────────────────────────────────────┤
│                                         │
│   ✅ 블록체인으로 투명성 확보            │
│   ✅ 게임화로 재미있는 경험              │
│   ✅ DID/VC로 증명 가능                  │
│   ✅ 누구나 쉽게 참여 가능               │
│                                         │
│   🚀 향후 NFT, 메타버스로 확장           │
│                                         │
│   감사합니다!                           │
└─────────────────────────────────────────┘
```

---

## 📂 발표 자료 파일 구성 제안

```
presentation/
├── slides.pptx              (메인 발표 자료)
├── demo-video.mp4           (데모 영상)
├── architecture.png         (아키텍처 다이어그램)
├── contract-code.png        (스마트 컨트랙트 코드 캡처)
├── did-example.json         (DID Document 예시)
├── vc-example.json          (VC 예시)
├── arbiscan-screenshot.png  (블록체인 탐색기)
└── roadmap.png              (향후 계획)
```

---

이 내용을 바탕으로 PPT를 만들면 됩니다!
각 섹션을 슬라이드로 변환하고, 코드/다이어그램/스크린샷을 적절히 배치하면
완벽한 발표 자료가 완성될 것 같습니다! 🎉
