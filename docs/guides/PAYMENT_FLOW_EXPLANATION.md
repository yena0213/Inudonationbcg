# 💰 결제 흐름 설명 - "사용자가 실제로 어떻게 돈을 내는가?"

## 🎯 핵심 요약

**현재 MVP 상태:**
- ❌ 실제 현금(KRW) 결제 시스템은 **구현되지 않음**
- ✅ 사용자는 **MetaMask 지갑의 ETH**로 직접 지불
- ✅ 테스트넷(Arbitrum Sepolia)이므로 **무료 테스트 ETH** 사용

**실제 프로덕션 시나리오:**
- 사용자가 미리 거래소(업비트, 빗썸)에서 ETH 구매 필요
- 또는 온램프(On-ramp) 서비스로 신용카드 결제

---

## 📊 현재 구현된 결제 흐름 (MVP)

### 1단계: 사용자 준비 (필수 선행 조건)

```
[사용자]
   │
   ├─ MetaMask 지갑 보유
   ├─ Arbitrum Sepolia 네트워크 추가
   └─ 테스트넷 ETH 보유 (Faucet에서 무료 획득)
        │
        └─ https://faucet.quicknode.com/arbitrum/sepolia
           → 0.1 ETH (무료)
```

**중요:** 테스트넷 ETH는 실제 가치가 없는 **가상의 ETH**입니다!

### 2단계: 기부 프로세스

```javascript
// 사용자: "3만원 기부하기" 버튼 클릭

// [1] Frontend: KRW를 ETH로 계산 (환율 적용)
const krwAmount = 30000;  // 3만원
const ethAmount = krwToEth(krwAmount);  // 0.01 ETH (1 ETH = 3,000,000 KRW 가정)

// [2] Frontend: Wei 단위로 변환
const valueInWei = ethers.parseEther(ethAmount);  // 10000000000000000 wei

// [3] Frontend: MetaMask 팝업 호출
const tx = await contract.donate(campaignId, message, {
  value: valueInWei,  // 0.01 ETH를 스마트 컨트랙트로 전송
  gasLimit: 300000
});

// [4] 사용자: MetaMask에서 "확인" 버튼 클릭
// → 이 순간 지갑에서 ETH가 빠져나감!

// [5] Blockchain: 트랜잭션 처리
// → 0.01 ETH가 스마트 컨트랙트로 이동
// → 스마트 컨트랙트가 즉시 단체 주소로 전송

// [6] Frontend: 트랜잭션 해시 받음
console.log('기부 완료:', tx.hash);
```

### 3단계: 실제 돈의 흐름 (블록체인)

```
[사용자 지갑]
Balance: 0.5 ETH
   │
   │ donate(campaignId, "응원합니다!", { value: 0.01 ETH })
   ▼
[스마트 컨트랙트]
Balance: 0.01 ETH (임시)
   │
   │ payable(organizationAddress).transfer(0.01 ETH)  ← 스마트 컨트랙트 내부
   ▼
[단체 지갑]
Balance: 0.01 ETH 증가
```

**Solidity 코드:**
```solidity
function donate(string memory campaignId, string memory message) 
    external 
    payable  // ← payable 키워드로 ETH 수신 가능
{
    require(msg.value > 0, "Donation must be greater than 0");
    
    // msg.value = 사용자가 보낸 ETH (wei 단위)
    // msg.sender = 사용자 지갑 주소
    
    // 기부 기록 저장
    donations.push(Donation({
        donor: msg.sender,
        campaignId: campaignId,
        amount: msg.value,  // 0.01 ETH (10^16 wei)
        timestamp: block.timestamp,
        message: message
    }));
    
    // 즉시 단체에게 전송
    address organizationAddress = campaigns[campaignId].organization;
    payable(organizationAddress).transfer(msg.value);
    
    emit DonationMade(msg.sender, campaignId, msg.value, block.timestamp);
}
```

---

## 🚨 현재 시스템의 문제점

### 문제 1: KRW → ETH 환전이 없음

**현재:**
```
사용자가 "3만원 기부" 버튼 클릭
  → 실제로는 0.01 ETH를 지불
  → 사용자는 미리 ETH를 보유하고 있어야 함!
```

**일반 사용자 입장:**
```
"나는 3만원을 기부하고 싶은데, ETH가 뭐야?"
"ETH를 어디서 사지?"
"왜 복잡하게 암호화폐로 기부해야 해?"
```

### 문제 2: 테스트넷 ETH는 가치가 없음

**현재 (Arbitrum Sepolia):**
- Faucet에서 무료로 받는 ETH
- 실제 가치 = 0원
- 단체도 테스트넷 ETH를 받음 → 현금화 불가능

**프로덕션 (Arbitrum Mainnet):**
- 실제 ETH 필요
- 1 ETH = 약 300만원 (2025년 기준)
- 단체가 받은 ETH를 거래소에서 현금화 가능

---

## ✅ 실제 프로덕션 시나리오 (3가지)

### 시나리오 1: 사용자가 직접 ETH 구매 (현실적)

```
[1] 사용자가 업비트/빗썸에 가입
     │
[2] 계좌 이체 3만원
     │
[3] 거래소에서 ETH 구매 (0.01 ETH)
     │
[4] ETH를 MetaMask 지갑으로 출금
     │
[5] 우리 플랫폼에서 기부
     │
[6] MetaMask로 0.01 ETH 전송
```

**장점:**
- ✅ 블록체인 투명성 확보
- ✅ 위변조 불가능

**단점:**
- ❌ 너무 복잡함 (일반인 진입장벽 높음)
- ❌ 거래소 수수료 발생
- ❌ 출금 시간 소요

### 시나리오 2: 온램프(On-ramp) 통합 (권장)

**온램프 = 신용카드로 바로 암호화폐 구매하는 서비스**

대표 서비스:
- Transak (https://transak.com/)
- MoonPay (https://www.moonpay.com/)
- Wyre

**구현 예시:**
```typescript
import { Transak } from '@transak/transak-sdk';

// 기부 버튼 클릭 시
function handleDonateWithCard() {
  const transak = new Transak({
    apiKey: 'YOUR_API_KEY',
    environment: 'PRODUCTION',
    
    // 사용자 정보
    walletAddress: userWalletAddress,
    
    // 구매 정보
    fiatCurrency: 'KRW',           // 한국 원화
    fiatAmount: 30000,             // 3만원
    cryptoCurrencyCode: 'ETH',     // ETH 구매
    network: 'arbitrum',           // Arbitrum 네트워크
    
    // 콜백
    onSuccess: (data) => {
      console.log('ETH 구매 완료!', data);
      // 자동으로 donate() 함수 호출
      executeDonation();
    }
  });
  
  transak.init();  // 팝업 열림
}
```

**사용자 경험:**
```
[1] "3만원 기부하기" 버튼 클릭
     │
[2] Transak 팝업 열림
     │
[3] 신용카드 정보 입력
     │
[4] KYC 인증 (처음 1회만)
     │
[5] 3만원 결제 (카드사 승인)
     │
[6] Transak이 자동으로 ETH 구매 후 지갑에 전송
     │
[7] 자동으로 donate() 함수 실행
     │
[8] 기부 완료!
```

**장점:**
- ✅ 사용자는 신용카드만 있으면 됨
- ✅ 거래소 가입 불필요
- ✅ 즉시 구매 가능

**단점:**
- ❌ 수수료 높음 (3~5%)
- ❌ KYC 인증 필요 (신분증)
- ❌ 서비스 통합 비용

### 시나리오 3: 하이브리드 (추천)

**일반 결제 + 블록체인 증명**

```
[1] 사용자가 신용카드/계좌이체로 3만원 결제
     │
     └─ 일반 PG사 (토스페이먼츠, 나이스페이)
     │
[2] 백엔드가 결제 확인
     │
[3] 백엔드가 관리하는 지갑으로 대신 기부
     │
     └─ await contract.donate(campaignId, message, { value: 0.01 ETH })
     │
[4] 트랜잭션 해시를 사용자에게 전달
     │
[5] 사용자는 Verifiable Credential 발급받음
```

**코드 예시:**
```typescript
// Backend (Supabase Edge Function)
app.post('/make-server-17e2e0df/donate-with-card', async (c) => {
  const { userId, campaignId, amount } = await c.req.json();
  
  // [1] 토스페이먼츠로 결제 처리
  const payment = await processPayment({
    amount: amount,  // 30000원
    orderId: `donation-${userId}-${Date.now()}`
  });
  
  if (!payment.success) {
    return c.json({ error: 'Payment failed' }, 400);
  }
  
  // [2] 백엔드 지갑으로 블록체인 기부 실행
  const adminWallet = new ethers.Wallet(
    Deno.env.get('ADMIN_PRIVATE_KEY'),
    provider
  );
  
  const contract = new ethers.Contract(
    CONTRACT_ADDRESS,
    DONATION_LEDGER_ABI,
    adminWallet
  );
  
  const ethAmount = krwToEth(amount);
  const tx = await contract.donate(campaignId, `${userId}님의 기부`, {
    value: ethers.parseEther(ethAmount)
  });
  
  await tx.wait();
  
  // [3] VC 생성 후 사용자에게 전달
  const credential = await createDonationCredential(
    userId,
    CONTRACT_ADDRESS,
    amount.toString(),
    1,
    campaignId,
    tx.hash
  );
  
  return c.json({
    success: true,
    txHash: tx.hash,
    credential: credential
  });
});
```

**장점:**
- ✅ 사용자는 일반 결제 방식 사용 (간편함)
- ✅ 블록체인 투명성 확보
- ✅ VC 발급으로 증명 가능

**단점:**
- ❌ 백엔드가 대신 기부 → 중앙화
- ❌ 관리 지갑 보안 중요
- ❌ 백엔드가 ETH를 보유해야 함

---

## 💡 발표용 설명

### Q: "사용자가 돈을 보내는 로직은? 뭘로 돈을 지불하는 거야?"

**A: 현재 MVP는 테스트 단계입니다.**

**현재 구조:**
1. 사용자는 **MetaMask 지갑**이 필요합니다
2. **테스트넷 ETH**를 Faucet에서 무료로 받습니다 (실제 가치 없음)
3. "3만원 기부" 버튼 클릭 시, 계산된 **ETH 양(0.01 ETH)**을 지불합니다
4. MetaMask 팝업에서 트랜잭션을 승인하면 **블록체인에 기록**됩니다

**실제 프로덕션 시:**
- **방법 1**: 사용자가 업비트 등에서 미리 ETH 구매
- **방법 2**: Transak 등 온램프로 신용카드 결제 (즉시 ETH 구매)
- **방법 3**: 일반 PG사 결제 후 백엔드가 대신 블록체인 기부

**핵심은:**
- 💳 **결제는 KRW** (사용자 친화적)
- ⛓️ **증명은 블록체인** (투명성 확보)
- 🏆 **VC 발급** (검증 가능한 자격증명)

이 3가지를 분리하여 **편의성과 투명성을 모두 확보**하는 것이 최종 목표입니다!

---

## 🔧 코드로 보는 실제 동작

### Frontend (DonationModal.tsx)

```typescript
// 사용자가 "3만원 기부" 버튼 클릭

const handleDonate = async () => {
  const donationAmount = 30000;  // KRW
  
  // [1] MetaMask 지갑 가져오기
  const signer = getEthereumSigner();
  
  // [2] 스마트 컨트랙트 객체 생성
  const contract = new ethers.Contract(
    CONTRACT_ADDRESS,
    DONATION_LEDGER_ABI,
    signer
  );
  
  // [3] KRW를 ETH로 변환
  const ethAmount = krwToEth(donationAmount);  // "0.01"
  const valueInWei = ethers.parseEther(ethAmount);  // 10000000000000000 wei
  
  // [4] MetaMask 팝업 호출 (사용자가 승인해야 함)
  const tx = await contract.donate(
    'unicef-children',
    '어린이들에게 희망을!',
    {
      value: valueInWei,  // ← 이 ETH가 지갑에서 빠져나감!
      gasLimit: 300000
    }
  );
  
  // [5] 트랜잭션 완료 대기
  const receipt = await tx.wait();
  
  console.log('기부 완료! txHash:', tx.hash);
};
```

### 환율 계산 (lib/contract.ts)

```typescript
// 1 ETH = 3,000,000 KRW로 가정
export function krwToEth(krwAmount: number): string {
  const ethPerKrw = 1 / 3000000;  // 1 KRW = 0.00000033 ETH
  const ethAmount = krwAmount * ethPerKrw;
  return ethAmount.toFixed(18);  // 0.01 ETH
}

// 예시:
krwToEth(30000)   → "0.010000000000000000"
krwToEth(100000)  → "0.033333333333333333"
krwToEth(3000000) → "1.000000000000000000"
```

### MetaMask 팝업 내용

```
┌─────────────────────────────────────┐
│        🦊 MetaMask                  │
├─────────────────────────────────────┤
│ DonationVillage가 요청함             │
│                                     │
│ 수신인: 0x1234...5678 (컨트랙트)     │
│ 금액: 0.01 ETH                       │
│ 가스비: 0.0001 ETH                   │
│ 합계: 0.0101 ETH                     │
│                                     │
│ [거부]          [확인] ←             │
└─────────────────────────────────────┘
```

사용자가 "확인"을 누르면:
- 지갑에서 0.0101 ETH 차감
- 블록체인에 트랜잭션 기록
- 영구적으로 취소 불가능

---

## 🎯 요약

| 구분 | 현재 MVP | 실제 프로덕션 |
|------|---------|--------------|
| **네트워크** | Arbitrum Sepolia (테스트넷) | Arbitrum Mainnet |
| **ETH** | 무료 (Faucet) | 실제 구매 필요 |
| **가치** | 0원 | 1 ETH = 300만원 |
| **결제 방법** | MetaMask 직접 | 온램프 또는 하이브리드 |
| **사용자 경험** | 복잡함 (암호화폐 지식 필요) | 간편함 (신용카드) |
| **목적** | 기술 검증 및 데모 | 실제 기부 플랫폼 |

**발표 시 강조할 점:**
1. 현재는 **기술 검증용 MVP**이므로 테스트넷 사용
2. 실제 서비스에서는 **온램프 통합** 또는 **하이브리드 방식** 적용 예정
3. 핵심은 **블록체인의 투명성**과 **사용자 편의성**의 균형!
